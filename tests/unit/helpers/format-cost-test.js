import { formatCost } from 'question/helpers/format-cost';
import { module, test } from 'qunit';

module('Unit | Helper | format cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatCost([42]);
  assert.ok(result);
});
